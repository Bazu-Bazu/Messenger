server:
  port: ${SERVER_PORT:8086}

spring:
  application:
    name: web-socket-service

frontend:
  port: "http://localhost:8000"

grpc:
  client:
    message-service:
      address: ${GRPC_CLIENT_MESSAGE_SERVICE_ADDRESS:static://message-service:6568}
      negotiation-type: plaintext