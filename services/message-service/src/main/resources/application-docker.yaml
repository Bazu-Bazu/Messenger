server:
  port: ${SERVER_PORT:8085}

spring:
  application:
    name: message-service

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres_message:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      connection-timeout: 30000
      max-lifetime: 300000
      idle-timeout: 30000
      leak-detection-threshold: 60000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:kafka:9093}

  cache:
    type: redis
    redis:
      time-to-live: 3m
      cache-null-values: false

  data:
    redis:
      host: ${SPRING_REDIS_HOST:redis}
      port: ${SPRING_REDIS_PORT:6379}

grpc:
  client:
    user-service:
      address: discovery:///user-service
      negotiation-type: plaintext
    group-chat-service:
      address: discovery:///group-chat-service
      negotiation-type: plaintext
    personal-chat-service:
      address: discovery:///personal-chat-service
      negotiation-type: plaintext

  server:
    port: ${GRPC_SERVER_PORT:6568}

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka

redis:
  ttl: 2