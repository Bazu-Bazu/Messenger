apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: messenger
  labels:
    app: media-service
spec:
  selector:
    app: media-service
  ports:
    - name: http
      port: 80
      targetPort: 8087