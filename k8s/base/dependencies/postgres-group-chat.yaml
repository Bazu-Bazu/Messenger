apiVersion: v1
kind: Service
metadata:
  name: postgres-group-chat
  namespace: messenger
spec:
  clusterIP: None
  ports:
    - port: 5432
      name: postgres
  selector:
    app: postgres-group-chat
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-group-chat
  namespace: messenger
spec:
  serviceName: "postgres-group-chat"
  replicas: 1
  selector:
    matchLabels:
      app: postgres-group-chat
  template:
    metadata:
      labels:
        app: postgres-group-chat
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_DB
              value: "group-chat_db"
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi