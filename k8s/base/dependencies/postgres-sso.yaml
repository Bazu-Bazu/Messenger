apiVersion: v1
kind: Service
metadata:
  name: postgres-sso
  namespace: messenger
spec:
  clusterIP: None
  ports:
    - port: 5432
      name: postgres
  selector:
    app: postgres-sso
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-sso
  namespace: messenger
spec:
  serviceName: "postgres-sso"
  replicas: 1
  selector:
    matchLabels:
      app: postgres-sso
  template:
    metadata:
      labels:
        app: postgres-sso
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_DB
              value: "sso_db"
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi