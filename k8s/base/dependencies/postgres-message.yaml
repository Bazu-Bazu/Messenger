apiVersion: v1
kind: Service
metadata:
  name: postgres-message
  namespace: messenger
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: postgres-message
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-message
  namespace: messenger
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-message
  template:
    metadata:
      labels:
        app: postgres-message
    spec:
      containers:
        - name: postgres-message
          image: postgres:16
          env:
            - name: POSTGRES_MESSAGE
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_DB
              value: "message_db"
          ports:
            - containerPort: 5432