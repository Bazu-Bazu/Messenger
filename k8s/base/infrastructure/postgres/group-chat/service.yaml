apiVersion: v1
kind: Service
metadata:
  name: postgres-group-chat
  namespace: messenger
spec:
  clusterIP: None
  ports:
    - port: 5432
      name: postgres
  selector:
    app: postgres-group-chat